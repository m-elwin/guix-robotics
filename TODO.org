1. ros-noetic-genpy tests are failing
   - I think this is because guix is running ctest
     ctest runs "make test"
     "make test" runs the nosetests
     it relies on files generated by "make tests".
2. Need to look into how catkin test handles this/what it invokes.
